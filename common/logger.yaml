logger:
  level: WARN
  on_message:
    level: ERROR
    then:
      - mqtt.publish:
          topic: ${topic_prefix}/error
          payload: !lambda |-
            return "Triggered on_message with level " + to_string(level) + ", tag " + tag + " and message " + message;